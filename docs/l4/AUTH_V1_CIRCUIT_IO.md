# AUTH_V1 Circuit IO

## Purpose
This note defines the current circuit-facing JSON layout for the `auth_v1` authorization proof path.

## Source
Built from:
- `artifacts/out_l4/proof_payload.auth_v1.json`

Generated by:
- `scripts/l4/build_auth_v1_circuit_input.py`

Output:
- `artifacts/out_l4/circuit_input.auth_v1.json`

## Public input order
1. `agentKey`
2. `capabilityId`
3. `policyClassHash`
4. `contextHash`
5. `traceCommitment`
6. `actionHash`
7. `expiryBucket`

## Private witness order
1. `scopeHash`
2. `capabilitySalt`
3. `actionCode`
4. `domainSepCapability`
5. `domainSepAction`

## Encoding rule
- raw hex values are preserved under `*_raw`
- decimal-string field values are emitted under `*_field`

This keeps the same content usable for:
- Solidity-side debugging
- circuit-side field input preparation
- future prover integrations

## Current status
This is still a pre-prover artifact.
It is the final deterministic bridge before introducing a real proving stack.
