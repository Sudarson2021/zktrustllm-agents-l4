# Generated consensus-comparison artifacts

This directory intentionally contains no numerical result until at least three
measured Ethereum-Sepolia/IoTeX-testnet sessions pass the strict analysis gate.

Generate the table, figure, results paragraph, summary, and derivation manifest with
`scripts/l4/consensus/analyze_pos_rolldpos.py`. Never enter experimental values by
hand.
