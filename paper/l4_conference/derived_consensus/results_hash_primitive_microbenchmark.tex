The local hash publication gate passed all retained-payload digest checks and the SHA-256/SHA-512 validation vectors. For the 32-byte input, the median primitive-path deltas were 59 gas for Keccak-256, 392 gas for the SHA-256 precompile path, and 162,174 gas for the pure-Solidity SHA-512 reference. For the 1,024-byte input, the median primitive-path deltas were 245 gas for Keccak-256, 2,879 gas for the SHA-256 precompile path, and 1,614,065 gas for the pure-Solidity SHA-512 reference. All 30 calls per algorithm/input cell returned the same delta because the bytecode, input, and local EVM state were fixed. These values characterise this isolated execution path; they are not public-testnet latency, monetary cost, finality, or consensus measurements.
